<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[垃圾收集]]></title>
    <url>%2F2019%2F01%2F17%2F%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%2F</url>
    <content type="text"><![CDATA[堆中几乎存放着Java世界中所有的对象实例，垃圾收集器在对堆进行回收前，第一件事情就是要确定这些对象有哪些还活着那些已经死去？ 引用计数法给对象中添加一个引用计数器，每当有一个地方引用它时，计数器值就+1，当引用失效时计数器值就-1，任何时刻计数器都为零的对象，就是不可能再被使用的。客观地说， 引用计数算法的实现简单判定效率也很高。在大部分情况下，他都是一个不错的算法，也有一些比较著名的应用案例，例如微软的COM技术等。Java语言中没有选用引用计数算法来管理内存， 其中最主要的原因是它很难解决对象之间的相互循环引用的问题。 根搜索算法在主流的商用语言中， 都是使用根搜索算法（GC Roots Tracing）判定对象是否存活的，从这些节点开始向下搜索， 搜索所通过的路径称为引用链，当一个对象到GC Roots没有任何引用链相连时，则证明此对象是不可用的。在Java语言里可作为GC Roots的对象包括下面几种： 虚拟机栈（栈帧中的本地变量表）中的引用的对象 方法区中的类静态属性引用的对象 方法区中的常量引用的对象 本地方法栈中JNI的引用的对象 再谈引用 强引用（Strong Reference)就是指在程序代码之中普遍存在的类似“Object obj = new Obejct()”这类的引用。 软引用（Soft Reference)用来描述一些还有用， 但并非必需的对象。 在系统将要发生内存溢出之前，将会把这些对象列进回收范围之中，并进行第二次回收，如果这次回收还是没有足够的内存才会抛出内存溢出异常。 弱引用（Weak Reference)也是用来描述非必需对象的，但他是强度比软引用更弱。一些被弱引用关联的对象只能生存到下一次垃圾收集发生之前，当垃圾收集器工作时，无论当前内存是否足够都会回收掉被弱引用关联的对象。 虚引用（Phantom Reference）也成为幽灵引用或者幻影引用，他是最弱的一种引用关系，一个对象是否有虚引用的存在，完全不对其生存时间构成影响，也无法通过虚拟用来获取一个对象的实例，为一个对象设置虚引用关联的唯一目的，就是希望能在这个对象被回收时收到一个系统通知。 在跟搜索算法中不可达的对象，也并非是非死不可的，这时候，他暂时处于缓刑阶段，要真正宣告一个对象死亡，至少要经历两次标记过程。如果对象在进行根搜索后，发现没有与GC Roots相连接的引用链，他将会被第一次标记，并且进行一次筛选，筛选的条件，是此对象是否有必要执行finalize()方法，当对象没有覆盖finalize()方法或者已经被虚拟机调用过，虚拟机将对这两种情况都视为没有必要执行。如果这个对象被判定为有必要执行finalize()方法，那么这个对象将会被放置在一个名为F-Queue队列之中，并在稍后由一条虚拟机自动创建的低优先级的线程去执行。这里所谓的执行是指虚拟机会触发这个方法，但并不承诺会等待他运行结束。这样做的原因是，如果一个对象在finalize()方法中执行缓慢或者发生了死循环，就很可能会导致F-Queue队列中其他对象永久处于等待状态，甚至导致整个内存回收系统崩溃。finalize()方法是对象逃脱死亡命运的最后一次机会，稍后GC将对F-Queue中的对象进行第二次小规模的标记， 如果对象要在finalize()方法中成功拯救自己—只要重新与引用链上的任何一个对象建立关联即可， 譬如把自己赋值给某个类变量或对象的成员变量上。 回收方法区永久代的垃圾收集主要回收两部分内容：废弃常量和无用的类，回收废弃常量与回收Java堆于中的对象非常相似，假如一个字符串“abc”已经进入了常量池，但是系统中没有任何一个String对象是叫做“abc”的， 如果这时候发生内存回收，而且必要的话这个常量就会被系统进出常量池，常量池中的其他类（接口）、方法、字段的符号引用也与此类似。判定一个常量是否是废弃常量比较简单，而判定一个类是否是无用的类的条件则相对困难许多需要，同时满足下面三个条件，才能算是无用的类。 所有的实例都已经被回收，也就是Java堆中不存在该类的任何实例。 加载该类的ClassLoader的已经被回收。 该类对应的java.lang.Class对象没有在任何地方被引用，无法在任何地方通过反射访问该类的方法。 虚拟机可以对满足上述三个条件的无用类进行回收，这里说的仅仅是可以而不是和对象一样，不使用了就必然会回收。是否对类进行回，HotSpot收虚拟机提供了-Xnoclassgc参数进行控制。在大量使用反射，动态代理、CGlib等bytecode框架的场景，以及动态生成JSP和OSGi这类频繁自定义ClassLoader的场景，都需要虚拟机具备类卸载的功能，以保证永代带不会溢出。 垃圾收集算法标记-清除算法最基础的收集算法是标记清除算法，和他的名字一样，算法分为标记和清除两个阶段，首先标记出所有需要回收的对象，在标记完成后，统一回收掉所有被标记的对象。他是最基础的收集算法，是因为后续的收集算法都是基于这种思路，并对其缺点进行改进而得到的。他的主要缺点有两个：一是效率问题，标记和清除过程的效率都不高，另外一个是空间问题，标记清除之后，会产生大量不连续的内存碎片，空间碎片太多，可能会导致动程序在以后的运行过程中需要分配较大对象时无法找到足够的连续内存而不得不提前触发一次垃圾收集动作。 复制算法为了解决效率问题，一种称为复制的收集算法出现了，他将可用内存容量划分为大小相等的两块，每次只使用其中的一块，当这一块的内存用完了，就将还存活的对象复制到另外一块上面，然后再把已使用过的内存空间一次清理掉。这样，使得每次都是对其中的一块进行内存回收，内存分配时也就不用考虑内存碎片等复杂情况。现在的商业虚拟机都采用这种收集算法来回收新生代。新生代中的对象98%是朝生夕灭的，所以并不需要按照一比一的比例来划分内存空间，而是将内存划为一块较大的Eden空间和两块较小的Survivor空间，每次使用Eden和其中的一块Survivor。当回收时，将还存活的对象一次性的拷贝到另外一块Survivor空间上，最后清理掉Eden和刚才用过的Survivor空间。 HotSpot虚拟机默认的Eden和Survivor的大小比例是8：1，也就是每次新生代中可用内存空间为整个新生代容量的90%只有10%的内存是被浪费的。我们没有办法保证每次回收都只有不多于10%的对象存活，当空间不够用时需要依赖其他内存进行分配担保。如果没有足够的空间，存放上一次新生代收集下来的存活对象。这些对象将直接通过分配担保机制进入老年代。 标记-整理算法复制收集算法的对象存活率较高时，就要执行较多的复制操作，效率将会变低，关键的是如果不想浪费50%的空间，就需要有额外的空间进行分配担保，以应对被使用的内存中对象都百分之百存活的极端情况，所以在老年代一般不能直接使用这种方法。根据老年代的特点，有人提出了另外一种标记整理，算法标记过程仍然与标记清除算法一样，但后续步骤不是直接对可回收对象进行清理，而是让所有存活的对象都像一端移动，然后直接清理掉边界以外的内存。 垃圾收集器Serial收集器是个单线程的收集器，他在垃圾收集时必须暂停其他所有工作线程，直到它收集结束 “Stop The World” ParNew收集器其实就是Serial收集器的多线程版本，除了使用多条线程进行垃圾收集之外，其余行为包括Serial收集器可用的所有控制参数、收集算法、Stop The World、对象分配规则、回收策略等都与Serial收集器完全一样。他是许多运行在Server模式下的虚拟机中首选的新生代收集器，其中有一个与性能无关，但很重要的原因是除了Serial收集器外，只有他能与CMS收集器配合工作。ParNew收集器在单CPU的环境中绝对不会有比Serial收集器更好的效果。 并发（Parallel）：指多条垃圾收集线程并行工作，但此时用户线程仍然处于等待状态 并行（Concurrent）：指用户线程与垃圾收集线程同时执行（但不一定是并行的，可能会交替执行）， 用户程序继续运行，而垃圾收集程序运行于另一个CPU上。 Parallel Scavenge收集器也是一个新生代收集器，它也是使用复制算法的收集器。他的目标是达到一个可控制的吞吐量，所谓吞吐量，就是CPU用于运行用户代码的时间与CPU总消耗时间的比值，即吞吐量 = 运行用户代码时间 / (运行用户代码时间 + 垃圾收集时间) Serial Old收集器Serial收集器的老年代版本，他同样是一个单线程收集器，使用标记-整理算法 Paralel Old收集器是Parallel Scavenge收集器的老年代版本，使用多线程和标记-整理算法。 CMS收集器是一种以获取最短回收停顿时间为目标的收集器，目前很大一部分的Java应用都集中在互联网的服务端上，这类应用尤其重视服务的响应速度，希望系统停顿时间最短，以给用户带来较好的体验。它基于标记-清除算法实现。运作过程： 初始标记 并发标记 重新标记 迸发清除 其中初始标记，重新标记这两个步骤仍然需要Stop The Wrold。初始标记，仅仅只是标记一下能直接GC Roots能关联到的对象，速度很快。并发标记阶段就是进行GC Roots Tracing的过程，而重新标记阶段是为了修正并发标记期间应用程序继续运行而导致标记产生变动的那一部分对象的标记记录。这个阶段的停顿时间一般会比初始标记阶段稍长一些，但远比并发标记的时间短。 缺点： 对CPU资源非常敏感，它虽然不会导致用户线程停顿，但是会因为占用了一部分线程资源而导致应用程序变慢，总吞吐量会降低。默认启动的回收线程是（CPU数量+3）/ 4， 当CPU在4个以上时最多占用不超过25%的的CPU资源。但是当CPU不足4个时，需要分出一半的运算能力去执行收集器线程。 无法处理浮动垃圾。由于并发清理阶段，用户线程还在运行，伴随程序的运行，自然还会有新的垃圾不断产生。这一部分垃圾出现在标记过程之后，CMS无法在本次收集中处理掉他们，只好留到下一次GC时再将其清理掉，这一部分垃圾就称为浮动垃圾。要是CMS运行期间预留的内存无法满足程序需要， 就会出现一次“Concurrent Mode Failure”失败，这时间虚拟机将启动后备预案：临时启用Serial Old收集器来重新进行老年代的垃圾收集， 这样停顿的时间就会很长了。 它是基于标记-清除算法实现的，空间碎片过多时会给大对象的分配带来很大麻烦，往往会出现老年代还有很大空间剩余，但无法找到足够大的连续空间来分配当前对象， 不得不提前触发一次Full GC。为了解决这个总是， CMS收集器提供了一个-XX:+UseCMSCompactAtFullCollection开关参数，用于在Full GC后额外附送一次碎片整理过程，内存整理过程是无法并发的。 G1收集器基于标记整理算法实现。它可以非常精确的控制停顿。他将整个Java堆 划分为多个大小固定的独立区域，并且跟踪这些区域里面的垃圾堆积程度，在后台维护一个优先列表。每次根据允许的收集时间优先回收垃圾最多的区域。 内存分配与回收策略 对象优先在Eden分配 大对象直接进入老年代虚拟机提供了一个-XX:PretenureSizeThreshold参数，大于这个值的对象直接进入老年代 长期存活的对象进入老年代对象在Survivor区每经过一次Minor GC，年龄就增加1岁，当增加到一定程度（默认15岁）就会晋升到老年代中。年龄阈值可以通过参数 -XX:MaxTenuringThreshold来设置 动态对象年龄判定为了更好适应不同程序内存情况， 虚拟机并不总是要求对象年龄必需达到MaxTenuringThreshold才能晋升老年代，如果在Survivor空间中相同年龄所有对象大小的总和大于Survivor空间的一半，年龄大于或等于该年龄的对象就可以直接进入老年代。 空间分配担保在发生Minor GC时，虚拟机会检测之前每次晋升到老年代的平均大小是否大于老年代的剩余空间大小，如果大于则改为直接进行一次Full GC。]]></content>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[内存泄漏与内存溢出]]></title>
    <url>%2F2015%2F08%2F17%2F%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BA%E4%B8%8E%E5%86%85%E5%AD%98%E6%B3%84%E6%BC%8F%2F</url>
    <content type="text"><![CDATA[内存泄漏内存泄漏是指无用对象（不再使用的对象）持续占有内存或无用对象的内存得不到及时释放，从而造成内存空间的浪费称为内存泄漏。 对象已经没有被应用程序使用，但是垃圾回收器没办法移除它们，因为还在被引用着。在Java中，内存泄漏就是存在一些被分配的对象，这些对象有下面两个特点，首先，这些对象是可达的，即在有向图中，存在通路可以与其相连；其次，这些对象是无用的，即程序以后不会再使用这些对象。如果对象满足这两个条件，这些对象就可以判定为Java中的内存泄漏，这些对象不会被GC所回收，然而它却占用内存。 内存溢出指程序运行过程中无法申请到足够的内存而导致的一种错误。内存溢出的几种情况 堆内存溢出 在jvm规范中，堆中的内存是用来生成对象实例和数组的。如果细分，堆内存还可以分为年轻代和年老代，年轻代包括一个eden区和两个survivor区。当生成新对象时，内存的申请过程如下： jvm先尝试在eden区分配新建对象所需的内存； 如果内存大小足够，申请结束，否则下一步； jvm启动youngGC，试图将eden区中不活跃的对象释放掉，释放后若Eden空间仍然不足以放入新对象，则试图将部分Eden中活跃对象放入Survivor区； Survivor区被用来作为Eden及old的中间交换区域，当老年代空间足够时，Survivor区的对象会被移到老年代，否则会被保留在Survivor区； 当老年代空间不够时，JVM会在老年代进行full GC； full GC后，若Survivor及老年代仍然无法存放从Eden复制过来的部分对象，导致JVM无法在Eden区为新对象创建内存区域，则出现：OutOfMemoryError：java heap space 方法区内存溢出 在jvm规范中，方法区主要存放的是类信息、常量、静态变量等。所以如果程序加载的类过多，或者使用反射、gclib等这种动态代理生成类的技术，就可能导致该区发生内存溢出，一般该区发生内存溢出时的错误信息为：OutOfMemoryError：permgem space 线程栈内存溢出 线程栈时线程独有的一块内存结构，所以线程栈发生问题必定是某个线程运行时产生的错误。一般线程栈溢出是由于递归太深或方法调用层级过多导致的。发生栈溢出的错误信息为：java.lang.StackOverflowError]]></content>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[JVM运行时数据区域]]></title>
    <url>%2F2015%2F08%2F17%2F%E8%BF%90%E8%A1%8C%E6%97%B6%E6%95%B0%E6%8D%AE%E5%8C%BA%E5%9F%9F%2F</url>
    <content type="text"><![CDATA[程序计数器程序计数器是一块较小的内存空间，它的作用可以看作是当前线程执行的字节码的行号指示器，在虚拟机的概念模型里字节码解释器工作时就是通过改变这个计数器选取下一条要执行的字节码指令，分支，循环，跳转异常处理，程序恢复等基础功能都需要依赖这个技术来完成。 由于JAVA虚拟机的多线程是通过线程轮流切换并分配处理器执行时间的方式来实现的，任何一个确定的时刻，处理器只执行一条程序中的指令。因此，为了线程切换后能恢复到正确执行位置，需要一个独立的程序的影响，独立存储区域为县城有内存 如果线程正在执行的是一个JAVA方法，这个计数器记录的是正在执行的虚拟机字节码指令的地址。如果正在执行的是native方法，这个计数器则为空 Java虚拟机栈和程序计数器一样，JAVA虚拟机栈也是线程私有的，它的生命周期与线程相同。每个方法被执行的时候会同时创建一个栈帧，用于存储局部变量表，操作数栈，动态链接方法出口等信息，每一个方法被调用，直到执行完成的过程，就对应着一个栈帧在虚拟机栈中，从入栈到出栈的过程。 局部变量表存放了，编译期可知的各种基本数据类型，其中64位长度的long和 double类型的数据会占用两个局部变量空间，局部变量表所需要的内存空间在编译期间完成分配，当进入一个方法时，这个方法需要在栈中分配多大的局部变量空间是完全确定的，在方法运行期间不会改变局部变量表的大小。 在java虚拟机规范中，对这个区域规定了两种异常情况，如果线程请求的栈深度大于虚拟机所允许的深度，将抛出StackOverflowError异常，如果虚拟机栈可以动态扩展，当扩展时无法申请到足够的内存时会抛出OutOfMemoryError异常。 本地方法栈本地方法栈Native Method Stacks与虚拟机栈所发挥的作用是非常相似的。其区别，不过是虚拟机栈为虚拟机执行JAVA方法服务，而本地方法栈则是位为虚拟机使用到的Native方法服务。 Java堆JAVA堆是被所有线程共享的一块内存区域，在虚拟机启动时创建。几乎所有对象实例都在这里分配内存。但是随着JIT编译器的发展与逃逸分析技术的逐渐成熟，栈上分与配标量替换优化技术将会导致一些微妙的变化发生，所有的对象都分配在堆上，也渐渐变得不是那么对了。 JAVA堆可以细分为新生代和老年代，再细致一点的有Eden空间，From Survivor空间，To Survivor空间等。 方法区方法区和JAVA堆一样是各个线程共享的内存区域，它用于存储已被虚拟机加载的类信息，静态变量，即时编译器编译后的代码等数据，虽然Java虚拟机规范，把方法区描述为堆的一个逻辑部分，但是它却有一个别名叫做Non-Heap(非堆)。对于习惯在HotSpot虚拟机上开发和部署程序的开发者来说，很多人愿意把方法区称为永久代， 本质上两者并不等价，仅仅是因为HotSpot虚拟机的设计团队选择把GC分段收集扩展至方法区或者说使用永久代来实现方法区而已。 运行时常量池运行时常量池是方法区的一部分，Class文件除了有类的版本、字段、方法、接口等描述信息外，还有一项信息是常量池，用于存放编译期生成的各种字面量和符号引用，这部分内容将在类加载后存放到方法区的运行时常量池中。 Java虚拟机对Class文件的每一个部分的格式都有严格的规定，每一个字节用于存储哪种数据都必须符合规范上的要求这样才会被虚拟机认可、装载和执行。但对于运行时常量池，Java虚拟机规范，没有做任何细节的要求，不同提供商实现的虚拟机，可以按照自己的需求来实现这个内存区域。不过一般来说，除了保存Class文件中描述的符号引用外，还会把翻译出来的直接引用也存储在运行时常量池中。运行常量池相对于Class文件常量池的另外一个重要特性是具备动态性，Java语言并不要求常量一定只能在编译期产生，也就是并非预置入class文件中常量词的内容才能进入方法区运行时常量池， 运行期间也可能将新加入的常量放入池中。这种特性被开发人员利用比较多的，便是String类的intern()方法。 直接内存直接内存并不是虚拟机运行时数据区的一部分，也不是Java虚拟机规范中定义的内存区域，但是这部分内存也被频繁的使用，而且也可能导致OutOfMemoryError异常出现。在JDK 1.4中新加入了NIO类，引入了一种基于通道与缓冲区的I/O方式，它可以使用native函数库直接分配对外内存，然后通过一个存储在Java堆里面的DirectByteBuffer对象作为这块内存的引用进行操作。这样能在一些场景中显著提高性能，因为避免了在Java堆和Native堆中来回数据复制。]]></content>
      <tags>
        <tag>JVM</tag>
      </tags>
  </entry>
</search>
